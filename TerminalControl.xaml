<UserControl x:Class="SimpleSshClient.TerminalControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <RichTextBox x:Name="OutputTextBox" 
                     Grid.Row="0"
                     IsReadOnly="True" 
                     Background="#1E1E1E" 
                     Foreground="#D4D4D4"
                     BorderThickness="0"
                     Padding="10"
                     FontFamily="Consolas"
                     FontSize="14"
                     VerticalScrollBarVisibility="Auto">
            <RichTextBox.Resources>
                <Style TargetType="Paragraph">
                    <Setter Property="Margin" Value="0"/>
                </Style>
            </RichTextBox.Resources>
        </RichTextBox>

        <Grid Grid.Row="1" Margin="5,5,5,5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="1.5*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <!-- 非对称排版 -->
            <Button x:Name="InterruptButton" Grid.Column="0" Content="中断" Click="InterruptButton_Click"/>
            <Button x:Name="CopyButton" Grid.Column="1" Content="复制" Click="CopyButton_Click"/>
            <Button x:Name="PasteButton" Grid.Column="2" Content="粘贴" Click="PasteButton_Click"/>
            <Button x:Name="SendButton" Grid.Column="3" Content="发送" Click="SendButton_Click"/>
            <Button x:Name="HistoryButton" Grid.Column="4" Content="记录" Click="HistoryButton_Click"/>
            <Button x:Name="SaveButton" Grid.Column="5" Content="另存" Click="SaveButton_Click"/>
            <Button x:Name="ClearButton" Grid.Column="6" Content="清屏" Click="ClearButton_Click"/>
        </Grid>

        <Grid Grid.Row="2" Background="#3C3C3C">
            <TextBox x:Name="InputTextBox" 
                     Background="Transparent" 
                     Foreground="#D4D4D4"
                     BorderThickness="0"
                     Padding="10,8"
                     FontFamily="Consolas"
                     FontSize="14"
                     KeyDown="InputTextBox_KeyDown"
                     ToolTip="输入命令后按Enter执行"/>
            <TextBlock x:Name="PlaceholderText" 
                       Text="输入命令后按Enter执行" 
                       Foreground="#888888" 
                       IsHitTestVisible="False" 
                       FontFamily="Consolas"
                       FontSize="14">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Setter Property="Visibility" Value="Collapsed"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding Text, ElementName=InputTextBox}" Value="">
                                <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding Text, ElementName=InputTextBox}" Value="{x:Null}">
                                <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding IsKeyboardFocused, ElementName=InputTextBox}" Value="True">
                                <Setter Property="Visibility" Value="Collapsed"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>
        </Grid>
    </Grid>
</UserControl>