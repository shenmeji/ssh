<Window x:Class="SimpleSshClient.Windows.DownloadProgressWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="下载进度"
        Height="200"
        Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="FileNameText" Grid.Row="0" Margin="0,0,0,10" FontWeight="SemiBold"/>
        
        <ProgressBar x:Name="ProgressBar" Grid.Row="1" Height="20" Margin="0,0,0,10"/>
        
        <TextBlock x:Name="ProgressText" Grid.Row="2" Margin="0,0,0,5" FontSize="12"/>
        
        <TextBlock x:Name="SpeedText" Grid.Row="3" Margin="0,0,0,5" FontSize="12"/>
        
        <TextBlock x:Name="TimeText" Grid.Row="4" Margin="0,0,0,15" FontSize="12"/>
        
        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="CancelButton" Content="取消" Padding="15,5" Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>