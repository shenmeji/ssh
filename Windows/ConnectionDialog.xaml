<Window x:Class="SimpleSshClient.Windows.ConnectionDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="快速连接" 
        SizeToContent="WidthAndHeight"
        MinWidth="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="连接名称:" Margin="0,0,0,5"/>
        <TextBox x:Name="NameTextBox" Grid.Row="1" Margin="0,0,0,10"/>

        <Grid Grid.Row="2" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">
                <TextBlock Text="主机地址:" Margin="0,0,0,5"/>
                <TextBox x:Name="HostTextBox" Margin="0,0,5,0"/>
            </StackPanel>

            <StackPanel Grid.Column="1">
                <TextBlock Text="端口:" Margin="0,0,0,5"/>
                <TextBox x:Name="PortTextBox" Text="22"/>
            </StackPanel>
        </Grid>

        <TextBlock Grid.Row="3" Text="用户名:" Margin="0,0,0,5"/>
        <TextBox x:Name="UsernameTextBox" Grid.Row="4" Margin="0,0,0,10"/>

        <StackPanel Grid.Row="5" Margin="0,0,0,10">
            <TextBlock Text="认证方式:" Margin="0,0,0,5"/>
            <RadioButton x:Name="PasswordRadio" Content="密码" IsChecked="True" Margin="0,0,0,5" Checked="AuthMethod_Checked"/>
            <PasswordBox x:Name="PasswordBox" Margin="0,0,0,10"/>
            <RadioButton x:Name="PrivateKeyRadio" Content="私钥文件" Margin="0,0,0,5" Checked="AuthMethod_Checked"/>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox x:Name="PrivateKeyPathTextBox" Grid.Column="0" IsReadOnly="True" Margin="0,0,5,0"/>
                <Button x:Name="BrowsePrivateKeyButton" Grid.Column="1" Content="浏览..." Click="BrowsePrivateKeyButton_Click"/>
            </Grid>
        </StackPanel>

        <StackPanel Grid.Row="7" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="SaveAndConnectButton" Content="保存连接" Margin="0,0,10,0" Padding="20,8" IsDefault="True" Click="SaveAndConnectButton_Click"/>
            <Button x:Name="CancelButton" Content="取消" Padding="20,8" IsCancel="True" Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
